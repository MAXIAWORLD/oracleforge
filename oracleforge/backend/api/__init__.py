"""MAXIA Oracle — public HTTP routes (Phase 3).

Each `routes_*` module exposes a FastAPI APIRouter that main.py mounts.
Keep one router per concern to keep modules short and testable in isolation.
"""
