[pytest]
# Shared config for running the 4 MAXIA Oracle plugin suites together.
# --import-mode=importlib avoids the basename collision on test_tools.py
# that happens when pytest is invoked with multiple plugin/tests paths at
# once (each plugin uses the same test module name).
addopts = --import-mode=importlib
