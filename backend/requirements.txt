# MAXIA Oracle — backend runtime dependencies (Phase 1)
#
# Phase 1 needs only the bare minimum to import and exercise the extracted
# oracle modules. The FastAPI server, MCP server, x402 middleware and test
# suite dependencies will be added in their respective phases.

httpx>=0.27.0,<0.28
python-dotenv>=1.0.1,<2.0
fastapi>=0.115.0,<0.117  # APIRouter is instantiated at module import in pyth_oracle
